{
  "buildCommand": "npm --prefix rondleiding install && npm --prefix rondleiding run build",
  "outputDirectory": "rondleiding/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://wipwapbackend.runasp.net/api/:path*"
    }
  ]
}
